# LAN IP of your PC (required for iPhone/Android on same Wi-Fi). Run: ipconfig
REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.10

# Local API (monolith or Docker gateway on :3000)
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000

# Railway staging API (datespot-staging project)
# EXPO_PUBLIC_API_URL=https://datespot-staging.up.railway.app

# EAS preview/staging builds use eas.json env — no .env needed on build servers.
# For local dev against staging, uncomment the Railway URL above.

# Required for the in-app Android map (Mapbox). Create a public token (pk.) at:
# https://account.mapbox.com/access-tokens/
# iPhone uses Apple Maps and does not need this key.
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Google Sign-In (optional). Without these, the "Continue with Google" button is
# hidden. Create OAuth client IDs at https://console.cloud.google.com/apis/credentials
# — iOS/Android client IDs need the bundle id / package + SHA-1 (Android); Web
# client id is used as the audience for id_token verification server-side.
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=

# Apple Sign-In needs no client id — it only requires the "Sign in with Apple"
# capability enabled for the app id in your Apple Developer account, and works
# automatically once that's on (button hides itself otherwise).
