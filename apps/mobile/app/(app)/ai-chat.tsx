/** AI date planner stack entry — same chat as the AI tab, with back navigation. */
import { AiChatView } from "../../src/components/AiChatView";

export default function AiChatScreen() {
  return <AiChatView showBack />;
}
